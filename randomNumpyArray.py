import numpy as np
import random

#Can produce floating values as we are using rand
array1=np.random.rand(5)
print(array1)

#Produces integer values as we are using randint values
random_array=np.random.randint(0,10,size=5)
print(random_array)